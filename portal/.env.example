# PILK portal — Vite env.
#
# Both values are public (anon key is safe to ship in the browser bundle).
# Copy to `.env.local` during dev; set as build-time env vars on Cloudflare
# Pages for production.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
